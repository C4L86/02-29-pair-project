# This class has been defined for users of the program
# The User class inherits ActiveRecord::Base
# Currently, we don't have any class methods defined, but they may need to be moved from controller actions for good coding practice
class User < ActiveRecord::Base

end
